# @lecipm/ui

Shared **React** UI primitives for BNHub / LECIPM (buttons, cards, layout shells).

- Consumed from `apps/web` via workspace dependency and TS path `@ui/*`.
- Move components here when they are **reused** across routes or apps; keep product-specific UI in `apps/web/components` until stable.

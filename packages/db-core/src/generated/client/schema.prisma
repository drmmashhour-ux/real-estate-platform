generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
  output     = "../src/generated/client"
}

datasource db {
  provider = "postgresql"
}

model PlatformConfig {
  id    String @id @default(cuid())
  key   String @unique
  value String
}

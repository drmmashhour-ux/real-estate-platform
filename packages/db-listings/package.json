{
  "name": "@repo/db-listings",
  "version": "0.0.0",
  "private": true,
  "description": "Staging package for Prisma model shards (listings bucket). Populate prisma/schema.prisma from enums.prisma + models.prisma after manual relation fixes."
}

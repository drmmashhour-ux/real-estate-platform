# Root pointer — copy app-specific examples and never commit real secrets.
# Main platform (Prisma, BNHub, LECIPM): see apps/web/.env.example
# Mobile (Expo): see apps/mobile/.env.example (EXPO_PUBLIC_*)
# Services: each service ships its own .env.example where applicable.

NODE_ENV=development

/**
 * Future: camera/microphone/contacts for broker workflows — v1 has no native permission gates.
 */
export function brokerMobilePermissionsNote(): string {
  return "No additional native permissions required for action center v1.";
}

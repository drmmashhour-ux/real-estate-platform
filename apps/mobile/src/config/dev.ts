/**
 * Set to `false` to restore Supabase session UI, `requireAuth`, and route guards.
 * Supabase client config in `lib/supabase.ts` stays unchanged when this is true.
 */
export const AUTH_DISABLED = false;

/** Placeholder — POST `/api/broker/internal-notes` with deal/contact scope. */
export function InternalNotesPanel() {
  return (
    <div className="rounded-xl border border-dashed border-ds-border p-4 text-center text-xs text-ds-text-secondary">
      Internal notes never leave brokerage-internal surfaces.
    </div>
  );
}

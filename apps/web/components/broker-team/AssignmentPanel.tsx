/** Placeholder — POST `/api/broker/assignments` from deal workspace. */
export function AssignmentPanel() {
  return (
    <div className="rounded-xl border border-dashed border-ds-border p-4 text-center text-xs text-ds-text-secondary">
      Assign coordinators or reviewers via the assignments API.
    </div>
  );
}

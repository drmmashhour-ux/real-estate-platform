/** Placeholder — use GET/POST `/api/broker/collaboration/threads` from deal or team contexts. */
export function CollaborationThreadPanel() {
  return (
    <div className="rounded-xl border border-dashed border-ds-border p-4 text-center text-xs text-ds-text-secondary">
      Thread composer ships with API v1; embed on deal pages next.
    </div>
  );
}

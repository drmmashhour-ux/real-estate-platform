import type { ReactNode } from "react";

/** Full-bleed mobile shell routes — no marketing Navbar/Footer. */
export default function MobileAppRoutesLayout({ children }: { children: ReactNode }) {
  return children;
}

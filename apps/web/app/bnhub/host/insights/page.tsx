import { redirect } from "next/navigation";

/** Bookmark-friendly entry → localized host insights. */
export default function BnhubHostInsightsAliasPage() {
  redirect("/en/ca/host/bnhub/insights");
}

/** TEMP: deployment proof — remove and restore `redirect("/listings")` after verifying app.lecipm.com serves this project. */
export default function Page() {
  return (
    <div style={{ padding: 40 }}>
      <h1>NEW DEPLOY WORKING ✅</h1>
      <p>If you see this, correct project is deployed.</p>
    </div>
  );
}

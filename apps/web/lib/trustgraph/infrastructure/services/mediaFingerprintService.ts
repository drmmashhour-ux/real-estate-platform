/**
 * Optional perceptual hash bridge — placeholder for future pHash integration.
 * Does not block Phase 6 when empty.
 */
export function placeholderPerceptualHash(_imageUrl: string): string | null {
  return null;
}

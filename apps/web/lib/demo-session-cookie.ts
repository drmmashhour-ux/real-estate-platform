/** Staging-only: Unix ms when the current demo session started (set on login). */
export const DEMO_SESSION_STARTED_AT_COOKIE = "lecipm_demo_session_ts" as const;

export const DEMO_SESSION_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 7;

/**
 * Short social / marketing drafts — premium tone; operator must verify claims against real listings.
 */
export function draftSocialSpotlight(city: string, listingCount: number): string {
  return `This week we’re featuring verified listings in ${city} (${listingCount} active in our sample). Explore fresh inventory and save your favorites — no hype, just transparent search.`;
}

/**
 * Canonical consent copy stored on every `InsuranceLead.consentText`.
 * Checkbox label must match this text (both sentences).
 */
export const INSURANCE_LEAD_CONSENT_TEXT =
  "I agree to be contacted by a licensed insurance broker regarding my request. You may be contacted by a licensed insurance broker.";

/** Shown next to the consent checkbox (identical to stored `consentText`). */
export const INSURANCE_LEAD_CONSENT_LABEL = INSURANCE_LEAD_CONSENT_TEXT;

/** Short trust line near the form (display only; not a substitute for `consentText`). */
export const INSURANCE_LEAD_TRUST_LINE =
  "Free quote · No obligation · Licensed Québec broker partners · Limited-time partner offers may apply.";

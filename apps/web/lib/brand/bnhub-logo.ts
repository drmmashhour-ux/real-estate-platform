/**
 * BNHUB lockup (gold 3D mark + wordmark) — asset in `/public/branding/`.
 * Use for BNHUB-only surfaces; LECIPM / main platform marks stay on `BrandLogo`.
 */
export const BNHUB_LOGO_SRC = "/branding/bnhub-logo.png" as const;

/** Compact mark for header / chips — replace with a square 192×192+ asset for best favicon/PWA results. */
export const BNHUB_MARK_SRC = "/branding/bnhub-mark.png" as const;

/**
 * Abuse prevention — presets and helpers. Core limits live in `@/lib/rate-limit` + Redis when configured.
 * @see `RATE_PRESETS` in `./rate-limit.ts`
 */
export { RATE_PRESETS } from "./rate-limit";

/**
 * @deprecated Prefer `@/lib/tracking` — re-exports for older imports.
 */
export {
  track as trackClientEvent,
  getTrackingSessionId,
  TRACKING_SESSION_KEY,
  TrackingEvent,
} from "@/lib/tracking";

import "server-only";

export async function computeHotZones(_limit: number): Promise<readonly unknown[]> {
  void _limit;
  return [];
}

export async function trendingAreasByRecentListings(_recentDays: number, _areas: number): Promise<readonly unknown[]> {
  void _recentDays;
  void _areas;
  return [];
}

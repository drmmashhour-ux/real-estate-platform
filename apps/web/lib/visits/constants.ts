/** Default IANA zone for Québec listings when broker has no per-row override. */
export const DEFAULT_VISIT_TIME_ZONE = "America/Toronto";

export const VISIT_SLOT_STEP_MINUTES = 15;

export const VISIT_DURATION_MIN = 30;
export const VISIT_DURATION_MAX = 60;
export const VISIT_DURATION_DEFAULT = 45;

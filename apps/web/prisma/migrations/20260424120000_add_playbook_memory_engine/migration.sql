-- Playbook Memory Engine — Prisma models at ~line 32357 in `schema.prisma` (`PlaybookMemoryRecord`, `MemoryPlaybook`, …).
-- If your database was provisioned with `prisma db push`, tables may already exist. In that case:
--   pnpm --filter @lecipm/web exec prisma migrate resolve --applied 20260424120000_add_playbook_memory_engine
-- On empty databases, use `prisma migrate deploy` after generating full DDL for your engine (e.g. `prisma migrate diff` against an empty DB), or `db push` for dev.
SELECT 1;

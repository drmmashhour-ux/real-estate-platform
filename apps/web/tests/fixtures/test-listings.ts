/**
 * Placeholder IDs for documentation — real simulations create ephemeral rows via `runStripeBookingE2e` or seed scripts.
 */
export const SIMULATION_LISTING_PLACEHOLDER = {
  note: "BNHub listing created/destroyed by stripe-booking-e2e.engine when live payment sim runs",
} as const;

/**
 * Revenue-domain signals stay in Revenue Autopilot (`lib/revenue-autopilot/*`).
 * Core autopilot only cross-links when needed — keep empty to avoid duplicate automation.
 */
export function evaluateRevenueAutopilotRules(): string[] {
  return [];
}

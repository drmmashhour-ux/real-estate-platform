export enum TrustBadgeVariant {
  Verified = "verified",
  AttentionNeeded = "attention_needed",
  NotReady = "not_ready",
}

export enum RiskHighlightSeverity {
  Info = "info",
  Warning = "warning",
  Blocker = "blocker",
}

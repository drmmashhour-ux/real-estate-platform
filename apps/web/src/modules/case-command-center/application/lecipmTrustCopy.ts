/**
 * Shared trust-first copy for LECIPM surfaces (non-legal, plain language).
 * Single place for deterministic fallbacks when AI or auxiliary paths fail.
 */

export const LECIPM_WORKFLOW_EVALUATE_FALLBACK =
  "We could not refresh workflow suggestions. Your file is unchanged — try again in a moment.";

/** Short line for sections that are secondary to the case snapshot primary next action. */
export const LECIPM_SUPPORTING_WORKFLOW_CAPTION =
  "Supporting checks from your file and rules. Your main next step is in “Next action” above.";

/** Reusable reminder for AI-assisted surfaces (not a substitute for professional advice). */
export const LECIPM_AI_ASSISTANT_REMINDER =
  "This is a planning assistant, not legal or tax advice. Confirm important details with your broker or notaire.";

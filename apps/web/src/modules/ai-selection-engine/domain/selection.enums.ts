export enum SelectionType {
  PROPERTY = "property",
  LEAD = "lead",
  ACTION = "action",
  STRATEGY = "strategy",
}

export enum PropertySelectionCategory {
  BEST_DEAL = "best_deal",
  SAFEST_DEAL = "safest_deal",
  BEST_CASHFLOW = "best_cashflow",
  BEST_LONG_TERM = "best_long_term",
  BEST_BNHUB_CANDIDATE = "best_bnhub_candidate",
}

export enum ActionRecommendation {
  CONTACT_NOW = "contact_now",
  VERIFY_DOCUMENTS = "verify_documents",
  ANALYZE_MORE = "analyze_more",
  WAIT = "wait",
  IGNORE = "ignore",
}

export enum StrategyRecommendation {
  RENT = "rent",
  FLIP = "flip",
  BNHUB = "bnhub",
  LIVE = "live",
  AVOID = "avoid",
}

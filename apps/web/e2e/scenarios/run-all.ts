/**
 * @deprecated Use `e2e/runner.ts` (`runAllScenarios`, `printE2ELaunchReport`).
 * Kept so relative imports from older docs still resolve.
 */
export { printE2ELaunchReport, runAll, runAllScenarios } from "../runner";

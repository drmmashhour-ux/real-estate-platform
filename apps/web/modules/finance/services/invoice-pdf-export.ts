/**
 * Placeholder for PDF export of tenant invoices (future: react-pdf / headless).
 */
export async function buildTenantInvoicePdfBuffer(_tenantInvoiceId: string): Promise<Buffer | null> {
  return null;
}

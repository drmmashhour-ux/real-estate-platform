/** Short copy for in-app “why this action” tooltips — no legal advice. */
export function explainDailyActionCenter(): string {
  return [
    "# Daily Action Center",
    "",
    "The feed ranks items from your **residential** deals, CRM, tasks, and compliance queue.",
    "Urgency uses real due dates and open statuses — it is not a sales gimmick.",
    "",
    "High-impact execution steps still belong in the **desktop deal workspace** when OACIQ rules apply.",
  ].join("\n");
}

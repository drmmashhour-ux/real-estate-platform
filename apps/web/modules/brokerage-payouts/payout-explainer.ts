export function payoutDisclaimer(): string {
  return (
    "Payout rows track intended disbursements. “Paid” is recorded when finance confirms receipt in-system; reconcile with bank statements externally."
  );
}

/**
 * Placeholder for structured diff between review snapshots — extend with document version hashes in v2.
 */
export function buildReviewDiffSummary(_before: unknown, _after: unknown): { summary: string } {
  return { summary: "Diff engine not yet wired — checklist + outcome capture operational audit trail." };
}

/** Stripe Checkout metadata: distinguishes LECIPM workspace SaaS from other subscription flows (e.g. mortgage expert). */
export const LECIPM_WORKSPACE_CHECKOUT = "lecipm_workspace_subscription";

/** Broker Pro / Platinum SaaS (lead priority + marketplace limits). */
export const LECIPM_BROKER_SUBSCRIPTION_CHECKOUT = "lecipm_broker_subscription";

import type { StrategySuggestion } from "./run-all-strategies";

export function openHouseStrategy(_ctx: unknown): StrategySuggestion[] {
  void _ctx;
  /** Optional: enable when visit scheduling is wired to listing context. */
  return [];
}

/**
 * Retention levers — policy decisions stay human-approved; no dark patterns.
 */
export function getBrokerRetentionActions(): string[] {
  return [
    "Give best leads to top brokers",
    "Reward fast responders",
    "Create elite tier access",
    "Limit high-quality leads to active brokers",
  ];
}

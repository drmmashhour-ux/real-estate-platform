/**
 * Ensures drafts do not imply brokerage capacity beyond mandate — surface-only check.
 */
export function representationDisclaimer(): string {
  return "Confirm agency relationship and disclosure obligations before any counter-proposal text is shared externally.";
}

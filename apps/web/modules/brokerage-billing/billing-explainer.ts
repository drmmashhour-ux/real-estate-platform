export function billingDisclaimer(): string {
  return (
    "Invoices are internal records for brokerage operations. Issued/paid states reflect actions recorded in LECIPM, not bank settlement confirmation unless you attach external references."
  );
}

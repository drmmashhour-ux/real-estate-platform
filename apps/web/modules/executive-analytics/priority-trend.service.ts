/**
 * Placeholder for time-series priority trends — v1 returns empty (requires persisted priority snapshots).
 */
export function priorityTrendStub(): { week: string; highCount: number }[] {
  return [];
}

export type ScaleMetric = {
  name: string;
  value: number;
  target: number;
};

export type ScalePlan = {
  revenueTarget: number;
  requiredLeads: number;
  avgPrice: number;
  brokerCount: number;
};

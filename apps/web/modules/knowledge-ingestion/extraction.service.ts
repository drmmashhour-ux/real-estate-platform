/**
 * Placeholder for structured extraction from uploaded references (future: OCR, table parse).
 * v1: broker-curated clause library + ingestion registry only.
 */
export async function extractStructuredFromUpload(_fileUrl: string): Promise<{ fields: Record<string, unknown> }> {
  return { fields: {} };
}

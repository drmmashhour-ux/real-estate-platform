/**
 * Placeholder for future internal benchmarks (e.g. rolling 90d median). Returns empty until backfilled.
 */
export function getInternalBenchmarkNote(): string {
  return "No external benchmarks — compare to prior period only.";
}

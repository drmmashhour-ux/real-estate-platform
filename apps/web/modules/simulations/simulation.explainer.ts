export function simulationNarrative(): string {
  return [
    "Montreal launch simulations aggregate host economics using configurable plan mix and uplift assumptions.",
    "Use for fundraising and sales planning — label all slides as modeled scenarios.",
  ].join(" ");
}

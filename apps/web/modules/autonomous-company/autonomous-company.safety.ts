/**
 * Hard safety bounds for autonomous company cycles — observational caps (no stored overrides).
 */

/** Max Operator plan rows considered per cycle (company layer). */
export const AUTONOMOUS_COMPANY_MAX_ACTIONS_PER_CYCLE = 5;

/** Upper bound for strategy / opportunity list sizes (defensive). */
export const AUTONOMOUS_COMPANY_MAX_OBJECTIVES = 12;

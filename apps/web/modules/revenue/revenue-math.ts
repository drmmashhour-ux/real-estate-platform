/** BNHub revenue math helpers — thin re-export (implementation in `modules/bnhub-revenue/`). */
export {
  addUtcDays,
  bnhubUtcWindowInclusiveEnd,
  round2,
  safeDivide,
  startOfUtcDay,
} from "../bnhub-revenue/bnhub-revenue-math";

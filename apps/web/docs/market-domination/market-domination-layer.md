# Market domination layer (advisory)

`GET /api/admin/market-domination` with `FEATURE_MARKET_DOMINATION_V1`.

Returns deterministic, explainable structures — ranking/pricing recommendations stay **advisory** until separately productised. No black-box scoring; services return empty arrays until wired to real signals.

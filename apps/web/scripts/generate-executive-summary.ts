/**
 * Generates executive summary PDF into /assets/investor.
 */
async function main() {
  console.log("[generate-executive-summary] Stub — wire PDF pipeline.");
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});

export {};

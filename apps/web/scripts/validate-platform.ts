/**
 * Backward-compatible entry: same as `platform-validation-v1.ts`.
 * Prefer: `pnpm validate:platform` → `scripts/platform-validation-v1.ts`
 * Legacy launch-events-only: `pnpm validate:platform:launch-events`
 */
import "./platform-validation-v1.ts";

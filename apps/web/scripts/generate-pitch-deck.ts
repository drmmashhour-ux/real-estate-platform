/**
 * Generates investor pitch deck assets into /assets/investor.
 * Replace stub with real template rendering when ready.
 */
async function main() {
  console.log("[generate-pitch-deck] Stub — wire deck generator and output path.");
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});

export {};

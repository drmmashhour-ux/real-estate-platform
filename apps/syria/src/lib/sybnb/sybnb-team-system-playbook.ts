/**
 * ORDER SYBNB-74 — Team operating model for Hadiah Link (scale without burning principals).
 * Headcount and activity targets are planning bands — adjust with hiring reality.
 */

/** Outbound sales — pipeline volume before closers take threads. */
export const SYBNB74_AGENTS_COUNT_MIN = 3;
export const SYBNB74_AGENTS_COUNT_MAX = 5;

/** Commercial conversion — hotels and upgrade conversations. */
export const SYBNB74_CLOSERS_COUNT_MIN = 1;
export const SYBNB74_CLOSERS_COUNT_MAX = 2;

/** Per-agent daily outbound touch target (DMs, calls, qualified replies initiated). */
export const SYBNB74_AGENT_OUTBOUND_CONTACTS_PER_DAY = 20;

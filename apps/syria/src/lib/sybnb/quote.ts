/**
 * SYBNB stay quote math — canonical exports live in `sybnb-quote.ts` (kept for V8 / imports stability).
 * Prefer importing from this file in new code.
 */
export { sybnbNightsBetween, computeSybnbQuote } from "./sybnb-quote";

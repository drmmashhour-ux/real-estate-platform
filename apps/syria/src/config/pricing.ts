/**
 * Darlink (Syria) — public pricing surface for the app lane.
 *
 * IMPORTANT: Completely separate from `apps/web` (Canada marketplace app) monetization.
 * Darlink uses simple listing fees + optional featured/boost in SYP — no broker commission model here.
 *
 * Canonical numbers live in `@/lib/pricing` (env-driven); this module is the preferred import path for clarity.
 */

export { SYRIA_PRICING } from "@/lib/pricing";

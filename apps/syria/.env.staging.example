# Syria (Darlink) — staging (placeholders only)
#
APP_ID=syria
APP_ENV=staging
INVESTOR_DEMO_MODE=false
STAGING_DATABASE_URL=postgresql://user:password@db.staging.example.com:5432/syria_sybnb_staging?sslmode=require
DATABASE_URL=postgresql://user:password@db.staging.example.com:5432/syria_sybnb_staging?sslmode=require
#
# Optional for local testing against “staging” on loopback
# ALLOW_LOCAL_STAGING_DB=true

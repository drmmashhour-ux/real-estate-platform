/**
 * Canonical guards live under `src/lib/assertContext.ts` (Next.js `src/` root).
 * This entry exists for tooling/docs that reference `apps/syria/lib/assertContext.ts`.
 */

export * from "../src/lib/assertContext";

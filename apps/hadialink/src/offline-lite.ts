/**
 * HadiaLink — lighter local-first shim (browse cache + outbound queue namespaces).
 */
export { enqueueAction, openOfflineDexie } from "@repo/offline";

export const HADIALINK_OFFLINE_NAMESPACE = "hadialink";

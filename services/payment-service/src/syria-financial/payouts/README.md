# Syria Payout Foundation

The payout module prepares future payout records behind `FEATURE_SYRIA_PAYOUTS`, which is off by default. Prepared payouts include merchant, provider, amount, idempotency key, correlation ID, and metadata.

No real transfer, bank instruction, or provider payout is executed.

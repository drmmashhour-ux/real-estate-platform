# Syria Payment Events

The events module normalizes future provider events into safe internal records. Events carry provider, provider event ID, type, transaction reference, correlation ID, status, redacted payload, and receive timestamp.

Webhook handling is stub-safe and does not activate provider connectivity.

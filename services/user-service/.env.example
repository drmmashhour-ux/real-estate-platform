# Same database as auth-service (users, user_roles, user_sessions). This service adds user_settings.
DATABASE_URL="postgresql://user:password@localhost:5432/lecipm_auth"

# Same JWT secret as auth-service (to verify access tokens)
JWT_ACCESS_SECRET="your-access-secret-min-32-chars"

PORT=3002
NODE_ENV=development

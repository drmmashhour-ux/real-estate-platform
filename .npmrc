# Ensure devDependencies install on CI/Vercel even when NODE_ENV=production during install.
# Required for workspace postinstall scripts (e.g. prisma generate) where prisma is a devDependency.
production=false
